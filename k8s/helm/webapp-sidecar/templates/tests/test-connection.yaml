apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webapp-sidecar.fullname" . }}-test-connection"
  labels:
    {{- include "webapp-sidecar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webapp-sidecar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
